import UIKit

class AppCoordinator: WindowCoordinator {
    
    override func start() {
        configure()
    }
    
    private func configure() {
        removeAllChilds()
        
        if UserDefaultValues.isLaunchSreenShown {
           
        } else {
           
        }
    }
}
